import { BoardElementsPipe } from './board-elements.pipe';

describe('BoardElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
